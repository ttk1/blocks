export class Block {
  private x: number;
  private y: number;
  private z: number;

  constructor(x: number, y: number, z: number) {
    // TODO
    this.x = x;
    this.y = y;
    this.z = z;
  }
}
